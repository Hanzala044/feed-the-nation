# Main Database credentials (CLIENT-SIDE - SAFE TO EXPOSE)
VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key-here"

# Auth credentials (CLIENT-SIDE - SAFE TO EXPOSE)
VITE_AUTH_SUPABASE_URL="https://your-auth-project.supabase.co"
VITE_AUTH_SUPABASE_ANON_KEY="your-auth-anon-key-here"

# ⚠️ SECURITY WARNING:
# Never commit the actual .env file to Git!
# Copy this file to .env and fill in your actual credentials
# The .env file is already in .gitignore
#
# ⚠️ DO NOT USE SERVICE_ROLE_KEY IN CLIENT CODE:
# Service role keys bypass Row Level Security and should NEVER be exposed to clients.
# Only use anon/publishable keys in frontend applications.
